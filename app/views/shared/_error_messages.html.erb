<% if @error_var.errors.any? %>
  <div id="error_explanation">
    <h3><%= pluralize(@error_var.errors.count, "error") %></h3>

    <ul>
      <% @error_var.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>